场景：根据登录的信息，后台返回对象的权限路由（即根据角色决定可以访问哪些页面）
思路：1）路由配置中设置路由规则，两个数组A和B，一个通用的页面匹配A(所有角色都能访问的)和非通用的页面匹配B（特定角色才能访问）
	2）在全局路由钩子router.beforeEach()中进行判断处理
	3）后台返回的角色信息中，有路由表，按照过滤规则生成新的路由表C
	4）通过A+C生成完整的权限路由
	5）router.addRoutes挂载到全局的router上
