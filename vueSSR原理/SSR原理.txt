优点：利于搜索引擎，解决白屏问题
场景：交互少，数据多，例如新闻，博客，论坛类等
原理：相当于服务端前面加了一层url分配，地址栏url改变或者直接刷新，是服务端渲染，其实就是选择性的从服务器返回内容，比如在首屏渲染或者直接刷新时，服务端返回的是一个html模板，而在交互过程中则是ajax处理操作，而通过a标签跳转则是在history模式下，通过history. pushState方式进而url改变，进行局部刷新
